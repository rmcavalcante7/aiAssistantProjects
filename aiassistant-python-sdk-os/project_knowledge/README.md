# Project Knowledge

This optional directory is for business-process documentation when the SDK wraps
real operational behavior.

Do not use it as an alternative to `.aiassistant`.

If the SDK is purely technical and has no process-heavy behavior, this directory
can remain unused.

